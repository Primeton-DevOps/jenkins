---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota
spec:
  hard:
    memory: ${memory}Gi
    cpu: '${cpu}'
    pods: '${pods}'
    services: '${services}'
    replicationcontrollers: '${replicationcontrollers}'
    resourcequotas: '${resourcequotas}'